//This function will hold the definitions for the functions required for pass 2 of the assembler


//? Should this loop over every line in the intermediate file?
void pass_2() {
    //call assemble_instructions(intermediate_file)
    //call process_vars()
    //call assembler_directives()
    //return output_file
    return; //temporary
}