//This will hold the function definitions for the functions needed in pass 1 of the assembler


//? Should this loop over every line in the input file?
void pass_1() {
    //call instruction_formats(input_file)
    //call instruction_addresses(input_file)
    //call calc_symtab(input_file)
    //call assembler_directives(input_file)
    //return intermediate_file
    return; //temporary
}
